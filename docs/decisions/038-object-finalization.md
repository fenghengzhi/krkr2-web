# 038 — 对象终结、主异常与深层释放

[最终报告 34868705139](https://github.com/fenghengzhi/krkr2-web/actions/runs/34868705139) 已在 GitHub 托管 runner 通过，绑定 530 份证据。标准矩阵 `out/verification/object-finalization-matrix.json` 的 SHA-256 为 `0387418a08e9a011d261937358510575a31f10061efaaff1e67c7ae910217d51`；TJS 原生源码 SHA-256 为 `6ec6de8e64ae16f23bc11008f3e159645a809ea9e795461879ab84537d85fbad`。正式构建标识为 `9e139eb5f451037c20f943e5d39294b6ab7d2c273e56963b0a5aae691cebb9cc`，发布树 32 文件的 SHA-256 为 `fee33376164f3aba10d731c4941800f93fda413b4ced8f8f48c1465c31cc89bd`。本轮可信冻结 21,055.1 ms，所选用例无跳过、flaky 或重试。

本地 `.generated` 与 `dist` 直接恢复自 Tests `34866740979` 的精确构建，未本地执行；上一阶段目录移动至 `out/verification/object-finalization/prior-local-artifacts/`。各 run 的完整产物与元数据保存在 `out/verification/github-actions/<run>/`，报告没有把未运行的额外冻结/冷重启或后续宿主所有权工作计为已通过。

兼容目标是 TJS2 的引用计数和显式失效语义。[原引擎调试文档](https://krkrz.github.io/krkr2doc/kr2doc/contents/Debug.html) 明确说明循环引用不会自动检测，应用可以用 `invalidate` 断开引用环；[类文档](https://krkrz.github.io/krkr2doc/tjs2doc/contents/class.html) 区分失效与删除，并未规定隐式终结的精确时间。此前进度文档把任意对象环的自动回收列为必需能力，这超出了参考行为；应验证原有引用/断环行为以及 Web 宿主资源的实际所有权，不能靠更换垃圾回收语义掩盖泄漏。

首轮 [诊断 34861822171](https://github.com/fenghengzhi/krkr2-web/actions/runs/34861822171) 复用已验证的执行预算构建，在独立子进程中检查源码/字节码、调试开关和两种后端。每个后端仅 4/32 通过：普通作用域退出正常；抛错终结器保留脚本上下文，闭包双引用释放在第一处错误后中断并触发 WASM abort，数组清理遗漏后续对象，构造错误被终结器异常覆盖。字典用例还误用了不存在的实例 `clear` 方法，修正为绑定到实例的 `Dictionary.clear`，并按字典缺失成员的 void 语义检查清空；该夹具错误与产品问题分别保留。

实现让 `Release` 在终结器失败时仍消费该次引用。对象若在终结器中被新所有者保留，则继续持有它；真正销毁时，基础对象释放剩余成员和原生实例，避免失败的 Finalize 遗留资源。闭包先分离两个指针，再尝试释放两份引用并保留第一处异常。数组/字典清理先预留引用列表，清空成员后完成全部释放；某一终结器失败不会跳过后续对象。

C++ 变体和作用域所有者的析构不能向 noexcept 容器外传播异常。它们把错误交给最内层清理作用域，在 VM 指令或执行返回边界报告；已有主异常时丢弃次要错误，避免用户 catch 后再出现同一清理错误。显式 `invalidate` 抛错仍保留原对象有效性，以允许按参考行为处理或重试；不能简单在任何 Finalize 入口就把对象标为无效。

引用计数导致的深层递归释放在 32 层后将最后一份引用转交给对象内的链表队列，不分配新的队列节点。最外层释放在返回前排空队列，避免纯 Array/Dictionary 长链绕过函数深度限制后耗尽原生栈。这改变深链中隐式终结的内部调度，不实现循环检测。队列长度、深度和累计入队量提供只读诊断，以验证长链确实经过该路径且最终没有待释放对象。

所有构建、测试和可执行探测只在 GitHub-hosted Actions 运行；此前完整基线与失败材料保留。宿主对象隐式资源回收与其他 VM 路径仍须继续实现，完整非插件目标保持进行中。

首轮修复的 Tests `34863624702` 在编译时失败：基础对象析构引用了源码后部才定义的 `GetValue` 宏。已改为显式读取符号的变体存储；该轮没有执行测试，编译日志和运行元数据完整保留。后续用例增加显式失效重试、终结器保留自身、显式断环、TJS catch 内的清理错误，以及两种终结方式的暂停/取消。原生对象存活计数与预热对照比较，用于发现对象仍存活但共享方法上下文数量未变化的泄漏。

Tests `34864904432` 构建成功，Node 为 462/466；四个失败均为 `caught-cleanup` 的调试/字节码组合，直接运行时也受该夹具影响。本地源码分析显示同一函数内的构造表达式仍有临时引用，`delete` 变量时未必触发析构；夹具改为在独立 setup 执行中创建全局唯一所有者，再在 try 内删除它，随后经下述 Actions 验证。清理分配诊断新增阶段 11，分别枚举 Array/Dictionary 的显式清空与隐式销毁，并在失败后检查终结次数、恢复执行、存活对象及独立分配账本。该阶段标记不承诺纯原生清理的协作暂停。

修订后 [Tests 34866740979](https://github.com/fenghengzhi/krkr2-web/actions/runs/34866740979) 的 14 个作业全部通过：466 项 Node、639 项浏览器及 6 项直接运行时。新增 68 项 Node 包含 60 个对象场景组合和 8 条显式/隐式终结的源码/字节码暂停与取消路径；三浏览器双后端直接运行时覆盖 360 个对象场景、48 条终结控制路径。[兼容专项 34867143808](https://github.com/fenghengzhi/krkr2-web/actions/runs/34867143808) 通过 78 项原 KAG 与跨 ABI 离线升级。[独立对象诊断 34867147315](https://github.com/fenghengzhi/krkr2-web/actions/runs/34867147315) 通过双后端 120/120，包括此前失败的全部 catch 组合。修订前独立诊断 `34865379657` 每个后端为 56/60，其失败日志和元数据继续保留。

[分配诊断 34866791836](https://github.com/fenghengzhi/krkr2-web/actions/runs/34866791836) 通过原有 188 次字节码和 1,063 次执行分配失败，以及新增 20 次集合清理分配失败。每个后端有 8 组调试开关/集合类型/清理方式组合，含成功对照、10 次实际失败和每组最后一个无故障终点。显式清空预留失败保留 128 个成员，重试后全部释放；隐式销毁失败仍释放全部成员。每例恢复执行后，VM 销毁的请求字节数、分配块数、字符串及对象数与对应成功对照一致。192 字节故障点位于 Array 隐式终结的附加清理，1,024 字节故障点是引用列表预留；未将此结果推广为全部原生分配或实时暂停保证。此前 `34865162293` 通过相同数量的旧分配用例，但未运行新增的清理阶段。

宿主所有权另需处理：例如当前 `ScriptEvents` 强持有绑定 Timer 实例的闭包；[原生 Timer](https://github.com/krkrz/krkr2/blob/master/kirikiri2/branches/2.32stable/kirikiri2/src/core/utils/TimerIntf.cpp) 的 `Owner` 仅保存实例指针，而 `ActionOwner` 才获取用户回调引用。这一差异不能用“原引擎不收集引用环”解释。下一阶段需覆盖宿主句柄释放队列的异常/重入，以及 Timer、AsyncTrigger、声音、图层等实际宿主资源的所有权，不能仅依赖脚本 `finalize` 或整场销毁。
