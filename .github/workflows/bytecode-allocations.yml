name: Bytecode allocation diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

env:
  CI: 'true'
  KRKR_TEST_ALLOCATOR: '1'

jobs:
  allocations:
    name: Allocator / ${{ matrix.variant }}
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        variant: [asyncify, jspi]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.19.0'
          cache: npm
      - run: npm ci
      - run: mkdir -p out/ci
      - name: Install native build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y bison cmake ninja-build python3
      - uses: emscripten-core/setup-emsdk@4528d102f7230f0e7b276855c01ea1159be0e984 # v16
        with:
          version: '6.0.9'
      - name: Build isolated diagnostic kernel
        run: node scripts/build-wasm.mjs "${{ matrix.variant }}" 2>&1 | tee out/ci/build.log
      - name: Fail each native allocation during pool construction, context construction and linking
        run: node --experimental-wasm-jspi --import tsx tests/probes/bytecode-allocation-faults.ts "${{ matrix.variant }}" 2>&1 | tee out/ci/allocations.log
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: bytecode-allocations-${{ matrix.variant }}
          path: |
            out/ci/
            .generated/wasm/
          include-hidden-files: true
          retention-days: 30
